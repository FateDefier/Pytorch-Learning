### 常用激活函数汇总（附核心特性+公式+适用场景）
激活函数是神经网络的“核心引擎”，用于引入非线性（否则多层网络等价于单层线性模型），以下是最常用的激活函数分类及详解：

#### 一、经典线性/非线性激活函数
| 激活函数       | 公式                  | 核心特性                                                                 | 适用场景                     | 缺点                          |
|----------------|-----------------------|--------------------------------------------------------------------------|------------------------------|-------------------------------|
| Sigmoid        | $σ(x) = 1/(1+e^{-x})$ | 输出∈(0,1)，可模拟“概率”；梯度∈(0,0.25)                                 | 二分类输出层、早期MLP        | 梯度消失（x±5以上梯度≈0）、非零均值 |
| Tanh（双曲正切）| $tanh(x) = (e^x-e^{-x})/(e^x+e^{-x})$ | 输出∈(-1,1)，零均值；梯度∈(0,1)（优于Sigmoid）                          | 隐藏层（替代Sigmoid）        | 仍存在梯度消失                |
| ReLU（修正线性单元） | $ReLU(x) = max(0, x)$ | 计算极快；梯度在x>0时=1（无梯度消失）；稀疏激活                          | 卷积神经网络（CNN）、MLP隐藏层 | 死亡ReLU（x≤0时梯度=0，参数不再更新） |

#### 二、ReLU变体（解决ReLU缺陷）
| 激活函数               | 公式                                  | 核心改进                                                                 | 适用场景                     |
|------------------------|---------------------------------------|--------------------------------------------------------------------------|------------------------------|
| Leaky ReLU             | $LeakyReLU(x) = max(αx, x)$（α≈0.01） | x<0时保留微小梯度（α≠0），避免死亡ReLU                                  | 深度学习通用（替代ReLU）     | α需手动调参                   |
| Parametric ReLU (PReLU)| $PReLU(x) = max(αx, x)$（α可学习）    | α由模型训练得到，适配不同任务                                           | 大规模数据集（如ImageNet）   | 增加少量参数                 |
| ELU（指数线性单元）| $ELU(x) = x (x>0); α(e^x-1) (x≤0)$    | x≤0时平滑衰减，零均值；鲁棒性优于ReLU                                   | 要求稳定性的网络（如GAN）    | 计算略复杂                   |
| Swish                  | $Swish(x) = x·σ(βx)$（β≈1）           | 非单调、平滑；在深层网络中精度优于ReLU                                  | Transformer、深层CNN         | 计算量略高                   |
| GELU（高斯误差线性单元） | $GELU(x) = x·Φ(x)$（Φ是高斯CDF）| 融合“随机正则”思想，更贴合自然语言特性                                  | Transformer（BERT/GPT核心）  | 需近似计算（常用$xσ(1.702x)$） |

#### 三、最新/专用激活函数
| 激活函数       | 核心特点                                                                 | 适用场景                     |
|----------------|--------------------------------------------------------------------------|------------------------------|
| Mish           | $Mish(x) = x·tanh(softplus(x))$（softplus=$ln(1+e^x)$）| 平滑、非单调；在CV任务中常优于Swish/ReLU | 目标检测、图像分类（CNN）    | 计算量较大                   |
| Softmax        | $Softmax(x_i) = e^{x_i}/Σe^{x_j}$（j为所有类别）| 输出∈(0,1)且和为1，可表示多分类概率     | 多分类任务输出层             | 易受极端值影响               |

#### 四、核心选型建议
1. 优先选：隐藏层用ReLU/Leaky ReLU（速度快、稳定）；Transformer用GELU；多分类输出层用Softmax；二分类输出层用Sigmoid；
2. 避坑：深层网络别用Sigmoid/Tanh（梯度消失）；小数据集别用PReLU（易过拟合）；
3. 调优：若ReLU出现“死亡神经元”，换Leaky ReLU（α=0.01~0.1）；追求高精度可试Swish/Mish（需容忍计算量增加）。