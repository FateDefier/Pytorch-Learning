# 优化器完全指南 —— 从梯度下降到 Adam

---

## 一、什么是优化器？为什么重要？

训练神经网络的目标是找到一组参数，使**损失函数最小化**。

```
损失函数 J(θ) = 预测值 与 真实值 的差距
目标：找到 θ* 使得 J(θ*) 最小
```

**优化器就是决定如何更新参数的那个算法。** 它回答了"往哪个方向走、走多远"的问题。好的优化器让你 10 轮收敛，差的让你 100 轮还在震荡。

---

## 二、核心概念速览

| 概念 | 含义 | 类比 |
|------|------|------|
| **梯度 (Gradient)** | 损失函数在当前位置的最陡上升方向 | 山坡的坡度 |
| **学习率 (lr)** | 每步沿梯度反方向走多远 | 步长 |
| **参数更新** | `θ ← θ − lr × ∇J(θ)` | 朝下山方向走一步 |
| **动量 (Momentum)** | 累加历史梯度，像"惯性" | 滚下山的球越滚越快 |
| **自适应学习率** | 不同参数用不同学习率 | 陡的地方小步，平的地方大步 |
| **Epoch** | 所有训练数据完整过一遍 | 把整本书读一遍 |

---

## 三、PyTorch 中使用优化器

```python
import torch.optim as optim

model = MyModel()
optimizer = optim.SGD(model.parameters(), lr=0.01, momentum=0.9)

for epoch in range(num_epochs):
    y_pred = model(x_data)
    loss = loss_fn(y_pred, y_data)

    optimizer.zero_grad()   # 清空上次的梯度
    loss.backward()         # 反向传播，计算新梯度
    optimizer.step()        # 用优化器更新参数
```

---

## 四、优化器全家福

### 4.1 SGD —— 随机梯度下降

```
参数更新：θ ← θ − lr × ∇J(θ)
加动量后：v ← γ·v + lr·∇J(θ)  ,  θ ← θ − v
```

```python
# 不带动量（收敛慢，震荡大）
optim.SGD(model.parameters(), lr=0.01)

# 带动量（几乎必加，大幅加速收敛）
optim.SGD(model.parameters(), lr=0.01, momentum=0.9)
```

| 优点 | 缺点 |
|------|------|
| 简单、可解释、泛化好 | 不自动调学习率，需手动调参 |
| 加动量后收敛速度和稳定性大幅提升 | 所有参数共享同一个学习率 |
| 几乎不占额外内存 | 训练初期震荡剧烈 |

> 💡 **"随机"是什么意思？** SGD 每次迭代从训练集中随机抽取部分样本（一个 batch）计算梯度，而非用全量数据。这让梯度带有噪声，但噪声反而帮助跳出局部最优。

---

### 4.2 Adagrad —— 自适应学习率的鼻祖

```
核心思想：频繁更新的参数学习率自动变小，稀疏参数保持大学习率

G ← G + (∇J)²              （累积梯度平方）
θ ← θ − lr / √(G + ε) × ∇J  （学习率被 1/√累积量 缩放）
```

```python
optim.Adagrad(model.parameters(), lr=0.01)
```

| 优点 | 缺点 |
|------|------|
| 适合稀疏数据（NLP 词向量等） | 学习率单调递减，后期→0 |
| 无需手动调学习率衰减 | 训练中后期参数几乎不再更新 |

> ⚠️ 在我们的线性回归实验（`y=2x`）中，Adagrad 表现最差（loss≈53，几乎没学到东西）。这不是 bug，而是因为线性回归的梯度方向稳定、累积快→学习率过早衰减到接近 0。将 lr 调至 0.1 或 1.0 即可改善。

---

### 4.3 RMSprop —— 修正 Adagrad 的衰减问题

```
核心改进：用"指数加权平均"替代"累积求和"，旧梯度影响逐渐衰减

v ← γ·v + (1−γ)·(∇J)²      （指数移动平均，γ≈0.9）
θ ← θ − lr / √(v + ε) × ∇J
```

```python
optim.RMSprop(model.parameters(), lr=0.01, alpha=0.99)
```

| 优点 | 缺点 |
|------|------|
| 解决了 Adagrad 学习率→0 的问题 | 对学习率仍敏感 |
| 适合非平稳目标（RNN 等） | 简单任务优势不明显 |

---

### 4.4 Adam —— 集大成者（最常用）

```
核心思想：同时追踪梯度的一阶矩（均值=m）和二阶矩（方差=v），
         并对二者做偏差校正

m ← β₁·m + (1−β₁)·∇J        （一阶矩，动量）
v ← β₂·v + (1−β₂)·(∇J)²      （二阶矩，自适应学习率）
m̂ ← m / (1−β₁ᵗ)              （偏差校正）
v̂ ← v / (1−β₂ᵗ)              （偏差校正）
θ ← θ − lr × m̂ / (√v̂ + ε)
```

```python
optim.Adam(model.parameters(), lr=0.001, betas=(0.9, 0.999))
```

| 优点 | 缺点 |
|------|------|
| 自适应：不同参数自动调学习率 | 有时泛化不如 SGD+momentum |
| 动量+自适应二合一 | 可能收敛到"不够好的"解 |
| 超参数不敏感，开箱即用 | 偶尔训练不稳定（有 AdamW 改进版） |

> 🏆 **Adam 是目前的默认选择**。不确定用什么优化器？先用 Adam。

---

### 4.5 Adamax —— Adam 的变体

```python
optim.Adamax(model.parameters(), lr=0.002)
```

- 与 Adam 的区别：用梯度的**无穷范数（最大值）**替代二阶矩
- 优点：对梯度极端值更鲁棒，适合嵌入层等梯度变化大的参数
- 在实验中收敛略慢于 Adam，但更稳定

---

### 4.6 ASGD —— 平均随机梯度下降

```
对 SGD 的每一步参数取累积平均，最终使用平均值作为模型参数

θ ← θ − lr × ∇J(θ)
θ̄ ← (t·θ̄ + θ) / (t+1)       （参数平滑平均）
```

```python
optim.ASGD(model.parameters(), lr=0.01)
```

- 与 Polyak 平均类似：不直接使用最终参数，而是使用训练过程中的平均参数
- 泛化能力更强，但前期收敛慢
- 适合需要强泛化的场景（如大规模 NLP）

---

### 4.7 Rprop —— 只看方向不看大小

```
核心思想：梯度只用来判断方向（符号），步长独立调整

若当前梯度 × 上一步梯度 > 0  →  步长 × 1.2（方向正确，加大步长）
若当前梯度 × 上一步梯度 < 0  →  步长 × 0.5（方向翻转，缩小步长）
θ ← θ − sign(∇J) × 步长
```

```python
optim.Rprop(model.parameters(), lr=0.01)
```

- 对梯度大小不敏感，只关心方向
- 适合梯度方向稳定的任务（如简单线性回归）
- ⚠️ 不适合 mini-batch 训练（batch 间梯度符号不稳定）

---

### 4.8 LBFGS —— 准牛顿法（二阶优化）

```
核心思想：利用历史梯度和参数更新近似海森矩阵（二阶导数），
        比一阶优化器收敛更快更准

仅存储最近 m 步的信息（L: Limited-memory），内存可控
```

```python
# LBFGS 使用方式与一阶优化器不同：需要闭包
optimizer = optim.LBFGS(model.parameters(), lr=1)

def closure():
    optimizer.zero_grad()
    loss = loss_fn(model(x), y)
    loss.backward()
    return loss

optimizer.step(closure)
```

- 收敛最快、精度最高（实验中 loss ≈ 2.5e-7）
- ⚠️ 必须使用全量数据（不支持 mini-batch），不能用于大规模深度学习
- 适合小规模问题、科学计算、微调最后几层

---

## 五、实战对比：8 种优化器在 y=2x 线性回归上的表现

**测试设置**：30 行数据，目标 `y=2x`（即 w=2, b=0, y_pred=8），epoch=50，默认超参数。

代码文件：[03. Stochastic_Gradient_Decent.py](03.%20Stochastic_Gradient_Decent.py)

### 结果总览

| 优化器 | 最终 Loss | w | b | y_pred (x=4) | 收敛评价 |
|:------:|:---------:|:------------:|:------------:|:-------------:|:--------:|
| **LBFGS** | **~2.5×10⁻⁷** | ~2.00 | ~−4×10⁻⁵ | **~8.00** | 🏆 极快极准 |
| **Rprop** | ~0.001 | ~1.98 | ~0.05 | ~7.96 | ⭐ 快速准确 |
| **SGD+动量** | ~0.01 | ~2.01 | ~−0.13 | ~7.91 | 加动量后质变 |
| **ASGD** | ~0.43 | ~1.56 | ~1.00 | ~7.24 | 前期慢后期稳 |
| **SGD** | ~0.055 | ~1.84 | ~0.36 | ~7.73 | 稳但慢 |
| **Adam** | ~4.68 | ~1.39 | ~0.11 | ~5.65 | 中等 |
| **Adamax** | ~10.06 | ~1.21 | ~−0.11 | ~4.73 | 略慢于 Adam |
| **RMSprop** | ~13.28 | ~0.75 | ~0.69 | ~3.70 | 不适合此场景 |
| **Adagrad** | ~53.20 | ~0.28 | ~−0.51 | ~0.60 | ⚠️ 学习率过早衰减 |

### 关键分析

1. **LBFGS 碾压：** 二阶方法利用曲率信息，在简单问题上精度远超一阶方法。但它用全量数据，大规模深度学习无法使用。

2. **Rprop 出彩：** 线性回归的梯度符号始终稳定（都要朝 w=2 的方向走），Rprop"只看方向"的策略完美适配。

3. **Adam 为什么不如 SGD？** 在这个简单任务上，Adam 的自适应学习率反而不如恒定学习率高效。但到了深层网络（10+ 层），Adam 的优势就会显现。

4. **Adagrad 翻车：** 学习率快速衰减到接近 0，50 轮后参数几乎原地不动。调大 lr 到 0.1~1.0 即可解决。

> 💡 **核心启示**：没有"最好的"优化器，只有最适合当前任务的。简单任务用 SGD+momentum 够用且泛化好，复杂任务用 Adam 省心。

---

## 六、选型速查表

| 场景 | 推荐优化器 | 原因 |
|------|:--------:|------|
| **通用默认** | **Adam** / **AdamW** | 开箱即用，超参数不敏感 |
| 追求最佳泛化（ImageNet 等） | **SGD + momentum** | 泛化能力经验上优于 Adam |
| Transformer / NLP | **AdamW** | 权重衰减与自适应解耦，训练更稳定 |
| GAN | **Adam** (β₁=0.5) | DCGAN 论文推荐配置 |
| 稀疏数据 | **Adagrad** 或 **Adam** (sparse) | 自适应处理稀疏梯度 |
| 简单凸优化/小规模 | **LBFGS** | 收敛极快，精度极高 |
| RNN / LSTM | **RMSprop** 或 **Adam** | 非平稳目标，自适应应对梯度剧烈变化 |

### 决策流程

```
规模多大？
├── 小规模（<1000 参数）、全量数据
│   └── LBFGS（极快极准，但仅限小规模）
│
├── 大规模深度学习
│   ├── 追求复现结果 / 发论文 → SGD + momentum（调 lr schedule）
│   ├── 追求省心 / 快速原型 → Adam / AdamW
│   └── Transformer 类 → AdamW
│
└── 不确定
    └── Adam（lr=0.001），然后根据结果调整
```

---

## 七、常见问题 FAQ

### Q1: Adam 和 AdamW 有什么区别？
AdamW 将**权重衰减（weight decay）和自适应学习率解耦**，让正则化更有效。简单说就是"更好、更稳定、更少过拟合的 Adam"。PyTorch 1.5+ 直接 `optim.AdamW()` 即可。

### Q2: 学习率怎么选？
- Adam/AdamW: `lr=0.001`（默认）
- SGD: `lr=0.01 ~ 0.1`（加 momentum=0.9）
- 经验法则：先设一个值，观察 loss 曲线；loss 震荡剧烈 → 减小 lr；loss 下降极慢 → 增大 lr

### Q3: 什么时候需要学习率衰减（lr scheduler）？
训练后期，如果 loss 震荡无法进一步下降，说明步长太大，在最优解附近"跳来跳去"。用 `torch.optim.lr_scheduler` 让学习率随训练递减。

```python
scheduler = optim.lr_scheduler.StepLR(optimizer, step_size=30, gamma=0.1)
for epoch in range(epochs):
    train()           # 训练一轮
    scheduler.step()  # 按策略衰减学习率
```

### Q4: 为什么实验中 Adam 不如 SGD？
这个实验只有 2 个参数（w 和 b），目标函数是简单的二次型。Adam 的自适应机制在这个尺度上反而引入了不必要的复杂性。**一阶优化器的差异在深层网络（数十万参数）中才会真正体现。**

---

## 参考

- [PyTorch 优化器文档](https://pytorch.org/docs/stable/optim.html)
- [Adam 论文](https://arxiv.org/abs/1412.6980)
- [AdamW 论文](https://arxiv.org/abs/1711.05101)
- [An overview of gradient descent optimization algorithms (Sebastian Ruder)](https://arxiv.org/abs/1609.04747)
